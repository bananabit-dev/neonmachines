document.addEventListener('DOMContentLoaded', () => {
    const tracingData = document.getElementById('tracing-data');
    tracingData.innerHTML = '<p>AI API tracing data will be displayed here.</p>';
});
